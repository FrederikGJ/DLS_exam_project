# `kubectl apply -k k8s/` = the airport system without dev tools (same as `kubectl apply -k k8s/base/`).
# Overlays: k8s/overlays/dev-tools (+ pgAdmin), k8s/overlays/demo (+ optional components) - see README.md.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base
