apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: airport
data:
  # Overrides js/config.js baked into the image: behind the Ingress all APIs are same-origin, and so is Keycloak
  # (/auth). KEYCLOAK_URL is built from the page's own origin, so it works for any host the Ingress is reached on;
  # note that the token issuer is still pinned by KC_HOSTNAME in keycloak/keycloak.yaml (see README.md).
  config.js: |
    window.AIRPORT_CONFIG = {
      FLIGHT_URL: '/api/flights/graphql',
      BOOKING_URL: '/api/bookings/graphql',
      PAYMENT_URL: '/api/payments/graphql',
      BAGGAGE_URL: '/api/baggage/graphql',
      BAGGAGE_REST_URL: '/api/baggage/v1',
      SHOP_URL: '/api/shops/graphql',
      KEYCLOAK_URL: window.location.origin + '/auth',
      KEYCLOAK_REALM: 'airport',
      KEYCLOAK_CLIENT_ID: 'airport-frontend'
    };
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: airport
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: airport
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend
        app.kubernetes.io/part-of: airport
    spec:
      containers:
        - name: nginx
          image: airport/frontend:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: config
              mountPath: /usr/share/nginx/html/js/config.js
              subPath: config.js
              readOnly: true
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: config
          configMap:
            name: frontend-config
