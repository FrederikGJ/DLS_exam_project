# DEV VALUES ONLY. Replace with a real secret (e.g. sealed-secrets / external-secrets) before any shared cluster.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
  labels:
    app.kubernetes.io/name: rabbitmq
    app.kubernetes.io/part-of: airport
type: Opaque
stringData:
  username: airport
  password: airport
